// Declarando uma tupla

let pessoa: [string, number, boolean]

pessoa = ['Cortez', 27, false]

let pivete: [string, boolean, number]

pivete = ['Matheus', true, 26]

console.log(pessoa)
console.log(pivete)